services:
  db:
    image: postgres:16-alpine
    container_name: market-research-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: market_research_mvp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d market_research_mvp"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

