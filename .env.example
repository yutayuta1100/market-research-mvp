# -----------------------------------------------------------------------------
# Current runtime: core app settings
# -----------------------------------------------------------------------------
NODE_ENV=development
APP_NAME=market-research-mvp
# Production should set APP_URL to the canonical HTTPS URL.
# Preview deployments can omit it and let the app derive from VERCEL_URL.
APP_URL=http://localhost:3000
PORT=3000
LOG_LEVEL=info
TIMEZONE=Asia/Tokyo

# -----------------------------------------------------------------------------
# Current runtime: database
# -----------------------------------------------------------------------------
# Milestone 2 runtime does not require a database connection, but optional
# snapshot persistence will use these when they are present.
# Keep these set locally if you want to use Docker Compose or Prisma commands.
# Vercel runtime writes need an external managed Postgres service.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/market_research_mvp?schema=public
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/market_research_mvp?schema=public

# -----------------------------------------------------------------------------
# Reserved for future milestones: auth / session
# -----------------------------------------------------------------------------
AUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Current runtime: feature flags
# -----------------------------------------------------------------------------
# Keep USE_MOCK_PROVIDERS=true for Development and Preview unless you are actively
# validating the public live-data path.
# When USE_MOCK_PROVIDERS=false, the app uses public social search, public Amazon
# search, and Yahoo! Auctions search as live reference sources.
# ENABLE_X_CONNECTOR only upgrades the social connector to the official X counts API
# when X_BEARER_TOKEN is also present.
USE_MOCK_PROVIDERS=true
ENABLE_X_CONNECTOR=false
ENABLE_AMAZON_CONNECTOR=false
ENABLE_KEEPA_CONNECTOR=false
ENABLE_SCHEDULED_JOBS=false
MOCK_WATCH_KEYWORDS=limited release,restock,best seller,price spike
MOCK_WATCH_CATEGORIES=Collectibles,Audio,Gaming,Power


# -----------------------------------------------------------------------------
# Current optional official social adapter: X
# -----------------------------------------------------------------------------
X_API_KEY=
X_API_SECRET=
X_BEARER_TOKEN=
X_CLIENT_ID=
X_CLIENT_SECRET=
X_DEFAULT_QUERY_WINDOW_DAYS=7
X_DEFAULT_LOCALE=ja
X_REQUEST_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Current live public-data controls: public search fetches
# -----------------------------------------------------------------------------
SOCIAL_REQUEST_TIMEOUT_MS=15000
AMAZON_MARKETPLACE=JP
AMAZON_REQUEST_TIMEOUT_MS=15000
MARKET_REQUEST_TIMEOUT_MS=15000
LIVE_DATA_REVALIDATE_SECONDS=1800

# -----------------------------------------------------------------------------
# Reserved for future official Amazon or partner API integrations
# -----------------------------------------------------------------------------
AMAZON_DEFAULT_CATEGORY=
AMAZON_ASSOCIATE_TAG=
AMAZON_ACCESS_KEY_ID=
AMAZON_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# Reserved for future official price-history provider integration
# -----------------------------------------------------------------------------
KEEPA_API_KEY=
KEEPA_DOMAIN=co.jp
KEEPA_REQUEST_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Current runtime: job runner / scheduling
# -----------------------------------------------------------------------------
# Used by /api/jobs/refresh when scheduled jobs are enabled.
CRON_SECRET=
JOB_CONCURRENCY=2
JOB_BATCH_SIZE=50
JOB_RETRY_LIMIT=3
JOB_RETRY_BACKOFF_MS=2000
REFRESH_CRON=0 */6 * * *

# -----------------------------------------------------------------------------
# Current runtime: scoring defaults
# -----------------------------------------------------------------------------
DEFAULT_PLATFORM_FEE_RATE=0.10
DEFAULT_SHIPPING_COST=750
DEFAULT_OTHER_COST=0
TREND_SCORE_WEIGHT=1.0
BESTSELLER_SCORE_WEIGHT=1.0
MARGIN_SCORE_WEIGHT=1.0
RISK_SCORE_WEIGHT=1.0
HIGH_MARGIN_THRESHOLD=0.20

# -----------------------------------------------------------------------------
# Reserved for future milestones: caching / queue / background infra
# -----------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379
CACHE_TTL_SECONDS=900

# -----------------------------------------------------------------------------
# Current runtime: storage / exports
# -----------------------------------------------------------------------------
EXPORT_DIR=./tmp/exports
UPLOAD_DIR=./tmp/uploads

# -----------------------------------------------------------------------------
# Reserved for future milestones: observability
# -----------------------------------------------------------------------------
SENTRY_DSN=
OTEL_EXPORTER_OTLP_ENDPOINT=

# -----------------------------------------------------------------------------
# Reserved for future milestones: email / notifications
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
ALERT_FROM_EMAIL=
ALERT_TO_EMAIL=

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1) Do not commit a real .env file.
# 2) Vercel Preview can safely run with mock mode enabled.
# 3) Production can run with USE_MOCK_PROVIDERS=false even without private API keys.
# 4) Set APP_URL in Vercel Production to the canonical site URL.
# 5) Scheduled refreshes require ENABLE_SCHEDULED_JOBS=true and a CRON_SECRET in production.
# 6) Add or remove variables as the implementation evolves, but keep this file in sync.
